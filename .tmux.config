# Enable vi mode. Enter with <prefix> ].
set-window-opetion -g mode-keys vi

# Change vi mode to use 'v' and 'y' for yanking.
bind-key -t vi-copy 'v' begin-selection
bind-key -t vi-copy 'y' copy-selection
